<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Mountain Meals – Freeze-Dried Peruvian Food</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@500;600;700&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">

  <style>
    :root {
      /* Brand palette */
      --sun-yellow: #F7C646;
      --sun-beige: #F4DA8A;
      --leaf-light: #A7C26F;
      --leaf: #6C8B37;
      --forest: #4E6627;
      --offwhite: #FDF9F0;
      --charcoal: #3A3A3A;

      --font-heading: "Quicksand", system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
      --font-body: "Inter", system-ui, -apple-system, BlinkMacSystemFont, sans-serif;

      --max-width: 1120px;
      --radius-lg: 18px;
      --radius-md: 12px;
      --shadow-soft: 0 14px 35px rgba(0, 0, 0, 0.06);
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: var(--font-body);
      color: var(--charcoal);
      background: #ffffff;
      line-height: 1.6;
    }

    img {
      max-width: 100%;
      display: block;
    }

    a {
      color: inherit;
      text-decoration: none;
    }

    .page {
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    .container {
      width: 100%;
      max-width: var(--max-width);
      margin: 0 auto;
      padding: 0 1.5rem;
    }

    /* NAVBAR */

    header {
      position: sticky;
      top: 0;
      z-index: 50;
      background: rgba(255, 255, 255, 0.96);
      backdrop-filter: blur(10px);
      border-bottom: 1px solid rgba(0, 0, 0, 0.04);
    }

    .nav {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0.75rem 0;
      gap: 1.5rem;
    }

    .nav-left {
      display: flex;
      align-items: center;
      gap: 0.75rem;
    }

    .logo-mark {
      width: 40px;
      height: 40px;
      border-radius: 999px;
      background: radial-gradient(circle at 30% 20%, #FFEFA7, var(--sun-yellow));
      border: 2px solid var(--leaf);
      position: relative;
      overflow: hidden;
    }

    .logo-mountain {
      position: absolute;
      bottom: -4px;
      left: 8px;
      width: 28px;
      height: 18px;
      border-left: 2px solid var(--leaf);
      border-bottom: 2px solid var(--leaf);
      border-right: 2px solid var(--leaf);
      transform: skewX(-12deg);
      border-radius: 4px 4px 0 0;
    }

    .logo-text {
      font-family: var(--font-heading);
      font-weight: 700;
      font-size: 1.1rem;
      letter-spacing: 0.06em;
      text-transform: uppercase;
    }

    .nav-links {
      display: flex;
      align-items: center;
      gap: 1.5rem;
      font-size: 0.95rem;
      font-weight: 500;
    }

    .nav-links a {
      position: relative;
      padding-bottom: 0.25rem;
    }

    .nav-links a::after {
      content: "";
      position: absolute;
      left: 0;
      bottom: 0;
      width: 0;
      height: 2px;
      background: var(--leaf);
      transition: width 0.18s ease-out;
    }

    .nav-links a:hover::after {
      width: 100%;
    }

    .nav-cta {
      display: flex;
      align-items: center;
      gap: 0.75rem;
    }

    .nav-cta button,
    .btn {
      border-radius: 999px;
      border: none;
      cursor: pointer;
      font-family: var(--font-heading);
      font-weight: 600;
      padding: 0.6rem 1.4rem;
      font-size: 0.95rem;
      transition: transform 0.12s ease-out, box-shadow 0.12s ease-out, background 0.12s ease-out, color 0.12s ease-out, border-color 0.12s ease-out;
    }

    .btn-primary {
      background: var(--forest);
      color: #fff;
      box-shadow: 0 10px 24px rgba(78, 102, 39, 0.28);
    }

    .btn-primary:hover {
      transform: translateY(-1px);
      box-shadow: 0 14px 30px rgba(78, 102, 39, 0.32);
    }

    .btn-secondary {
      background: transparent;
      border: 1.5px solid var(--leaf);
      color: var(--leaf);
    }

    .btn-secondary:hover {
      background: var(--offwhite);
    }

    .language-pill {
      border-radius: 999px;
      border: 1px solid rgba(0, 0, 0, 0.06);
      padding: 0.25rem 0.75rem;
      font-size: 0.8rem;
      display: flex;
      align-items: center;
      gap: 0.3rem;
      background: #ffffff;
    }

    .language-pill span:first-child {
      font-weight: 600;
      color: var(--forest);
    }

    @media (max-width: 840px) {
      .nav-links {
        display: none;
      }
      .nav-cta button {
        padding-inline: 1rem;
      }
    }

    /* HERO */

    .hero {
      background: radial-gradient(circle at top left, #FFF9E5, #FFFFFF);
      padding: 3.25rem 0 2.75rem;
    }

    .hero-grid {
      display: grid;
      grid-template-columns: minmax(0, 3fr) minmax(0, 2.5fr);
      gap: 3rem;
      align-items: center;
    }

    .hero-eyebrow {
      text-transform: uppercase;
      letter-spacing: 0.18em;
      font-size: 0.72rem;
      font-weight: 600;
      color: var(--leaf);
      margin-bottom: 0.75rem;
    }

    .hero-title {
      font-family: var(--font-heading);
      font-weight: 700;
      font-size: clamp(2.2rem, 3vw + 1rem, 3rem);
      letter-spacing: 0.10em;
      text-transform: uppercase;
      line-height: 1.15;
      margin-bottom: 1rem;
      color: var(--forest);
    }

    .hero-subtitle {
      font-size: 1rem;
      max-width: 34rem;
      color: #505050;
      margin-bottom: 1.75rem;
    }

    .hero-actions {
      display: flex;
      flex-wrap: wrap;
      gap: 0.9rem;
      margin-bottom: 1.5rem;
    }

    .hero-note {
      font-size: 0.82rem;
      color: #7A7A7A;
    }

    .hero-visual {
      position: relative;
      min-height: 260px;
    }

    .hero-sun {
      width: 260px;
      height: 260px;
      border-radius: 999px;
      background: radial-gradient(circle at 30% 15%, #FFF6C3, var(--sun-yellow));
      margin-left: auto;
      box-shadow: var(--shadow-soft);
      border: 3px solid var(--leaf);
      position: relative;
      overflow: hidden;
    }

    .hero-mountains {
      position: absolute;
      bottom: 18px;
      left: 40px;
      width: 180px;
      height: 110px;
      border-bottom: 3px solid var(--leaf);
      border-left: 3px solid transparent;
      border-right: 3px solid transparent;
      transform: skewX(-14deg);
      border-radius: 8px 8px 0 0;
    }

    .hero-label {
      position: absolute;
      right: 0;
      bottom: 0;
      transform: translate(20%, 40%);
      background: #ffffff;
      border-radius: 999px;
      padding: 0.4rem 0.9rem;
      font-size: 0.75rem;
      box-shadow: var(--shadow-soft);
      border: 1px solid rgba(0, 0, 0, 0.04);
      display: flex;
      align-items: center;
      gap: 0.4rem;
    }

    .hero-label-dot {
      width: 10px;
      height: 10px;
      border-radius: 999px;
      background: var(--sun-yellow);
      border: 1px solid var(--leaf);
    }

    @media (max-width: 840px) {
      .hero {
        padding-top: 2.25rem;
      }
      .hero-grid {
        grid-template-columns: minmax(0, 1fr);
        gap: 2.5rem;
      }
      .hero-visual {
        order: -1;
        justify-self: center;
      }
      .hero-sun {
        margin: 0 auto;
      }
    }

    /* FEATURES / ICONS SECTION */

    .features {
      padding: 2.25rem 0 2.5rem;
    }

    .feature-grid {
      display: grid;
      grid-template-columns: repeat(4, minmax(0, 1fr));
      gap: 1.4rem;
    }

    .feature-card {
      background: var(--offwhite);
      border-radius: var(--radius-md);
      padding: 1rem 1rem 1.1rem;
      border: 1px solid rgba(0, 0, 0, 0.03);
      box-shadow: 0 10px 26px rgba(0, 0, 0, 0.03);
    }

    .feature-icon {
      width: 34px;
      height: 34px;
      border-radius: 999px;
      border: 2px solid var(--leaf);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.1rem;
      margin-bottom: 0.6rem;
      background: #ffffff;
    }

    .feature-title {
      font-family: var(--font-heading);
      font-size: 0.9rem;
      font-weight: 600;
      margin-bottom: 0.25rem;
    }

    .feature-text {
      font-size: 0.82rem;
      color: #666666;
    }

    @media (max-width: 960px) {
      .feature-grid {
        grid-template-columns: repeat(2, minmax(0, 1fr));
      }
    }

    @media (max-width: 640px) {
      .feature-grid {
        grid-template-columns: minmax(0, 1fr);
      }
    }

    /* FEATURED MEALS */

    .section-heading {
      font-family: var(--font-heading);
      font-size: 1.2rem;
      font-weight: 600;
      margin-bottom: 0.35rem;
      color: var(--forest);
    }

    .section-subtitle {
      font-size: 0.9rem;
      color: #6A6A6A;
      margin-bottom: 1.25rem;
    }

    .meals {
      padding: 0.75rem 0 2.5rem;
    }

    .meal-grid {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 1.5rem;
    }

    .meal-card {
      background: #ffffff;
      border-radius: var(--radius-lg);
      overflow: hidden;
      border: 1px solid rgba(0, 0, 0, 0.04);
      box-shadow: var(--shadow-soft);
      display: flex;
      flex-direction: column;
    }

    .meal-thumb {
      background: linear-gradient(135deg, #FFF4C4, #F3E7C8);
      height: 150px;
      position: relative;
    }

    .meal-thumb-tag {
      position: absolute;
      left: 12px;
      top: 12px;
      background: rgba(255, 255, 255, 0.92);
      padding: 0.3rem 0.7rem;
      border-radius: 999px;
      font-size: 0.75rem;
      font-weight: 500;
    }

    .meal-body {
      padding: 0.9rem 1rem 1rem;
      display: flex;
      flex-direction: column;
      gap: 0.3rem;
      flex: 1;
    }

    .meal-title {
      font-family: var(--font-heading);
      font-size: 1rem;
      font-weight: 600;
    }

    .meal-meta {
      font-size: 0.82rem;
      color: #7A7A7A;
    }

    .meal-footer {
      margin-top: 0.6rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 0.5rem;
      font-size: 0.82rem;
    }

    .meal-link {
      font-weight: 500;
      color: var(--leaf);
      cursor: pointer;
    }

    .meal-price {
      font-weight: 600;
    }

    @media (max-width: 960px) {
      .meal-grid {
        grid-template-columns: repeat(2, minmax(0, 1fr));
      }
    }

    @media (max-width: 640px) {
      .meal-grid {
        grid-template-columns: minmax(0, 1fr);
      }
    }

    /* MEAL PLAN CTA */

    .planner {
      padding: 2.75rem 0 2.75rem;
      background: radial-gradient(circle at top right, #FFF6C6, #FFFFFF);
      border-top: 1px solid rgba(0, 0, 0, 0.03);
      border-bottom: 1px solid rgba(0, 0, 0, 0.03);
    }

    .planner-inner {
      border-radius: 22px;
      padding: 2rem 1.5rem;
      background: #ffffff;
      box-shadow: var(--shadow-soft);
      position: relative;
      overflow: hidden;
    }

    .planner-tag {
      font-size: 0.75rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.14em;
      color: var(--leaf);
      margin-bottom: 0.5rem;
    }

    .planner-title {
      font-family: var(--font-heading);
      font-size: 1.4rem;
      margin-bottom: 0.4rem;
      color: var(--forest);
    }

    .planner-text {
      font-size: 0.9rem;
      max-width: 32rem;
      margin-bottom: 1.1rem;
      color: #555555;
    }

    .planner-meta {
      font-size: 0.8rem;
      color: #7C7C7C;
      margin-top: 0.6rem;
    }

    .planner-bg-sun {
      position: absolute;
      right: -40px;
      bottom: -40px;
      width: 200px;
      height: 200px;
      border-radius: 999px;
      border: 18px solid rgba(247, 198, 70, 0.45);
      opacity: 0.55;
    }

    /* STORY */

    .story {
      padding: 2.75rem 0 2.75rem;
    }

    .story-grid {
      display: grid;
      grid-template-columns: minmax(0, 1.3fr) minmax(0, 1.6fr);
      gap: 2.5rem;
      align-items: center;
    }

    .story-illustration {
      background: radial-gradient(circle at top, #FFF6D9, #FFFFFF);
      border-radius: 22px;
      padding: 2rem 1.5rem;
      border: 1px solid rgba(0, 0, 0, 0.04);
      position: relative;
    }

    .story-mountains {
      width: 70%;
      max-width: 260px;
      height: 120px;
      border-bottom: 4px solid var(--leaf);
      border-left: 4px solid transparent;
      border-right: 4px solid transparent;
      margin: 2.5rem auto 0;
      transform: skewX(-14deg);
      border-radius: 10px 10px 0 0;
    }

    .story-sun {
      position: absolute;
      top: 24px;
      left: 50%;
      transform: translateX(-50%);
      width: 140px;
      height: 140px;
      border-radius: 999px;
      background: radial-gradient(circle at 30% 20%, #FFF6C3, var(--sun-yellow));
      border: 3px solid var(--leaf);
      box-shadow: var(--shadow-soft);
    }

    .story-label {
      position: absolute;
      left: 1.5rem;
      bottom: 1.5rem;
      background: #ffffff;
      border-radius: 999px;
      padding: 0.35rem 0.9rem;
      font-size: 0.78rem;
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      box-shadow: var(--shadow-soft);
    }

    .story-label-pill {
      width: 18px;
      height: 18px;
      border-radius: 999px;
      border: 2px solid var(--leaf);
      background: var(--leaf-light);
    }

    .story-heading {
      font-family: var(--font-heading);
      font-size: 1.35rem;
      margin-bottom: 0.6rem;
      color: var(--forest);
    }

    .story-body {
      font-size: 0.92rem;
      color: #555555;
      margin-bottom: 1rem;
    }

    .story-link {
      font-size: 0.9rem;
      font-weight: 500;
      color: var(--leaf);
      display: inline-flex;
      align-items: center;
      gap: 0.25rem;
    }

    .story-link::after {
      content: "→";
      font-size: 0.9rem;
    }

    @media (max-width: 900px) {
      .story-grid {
        grid-template-columns: minmax(0, 1fr);
      }
    }

    /* FOOTER */

    footer {
      padding: 1.6rem 0 1.9rem;
      border-top: 1px solid rgba(0, 0, 0, 0.04);
      background: #ffffff;
      margin-top: auto;
    }

    .footer-row {
      display: flex;
      flex-wrap: wrap;
      gap: 1.5rem;
      align-items: center;
      justify-content: space-between;
      font-size: 0.82rem;
      color: #777777;
    }

    .footer-nav {
      display: flex;
      gap: 1.1rem;
      flex-wrap: wrap;
    }

    .footer-nav a {
      text-decoration: none;
    }

    .footer-nav a:hover {
      color: var(--forest);
    }

    .footer-note {
      font-size: 0.78rem;
      color: #9A9A9A;
    }
  </style>
</head>

<body>
  <div class="page">
    <!-- NAVBAR -->
    <header>
      <div class="container">
        <nav class="nav">
          <div class="nav-left">
            <div class="logo-mark">
              <div class="logo-mountain"></div>
            </div>
            <div class="logo-text">Mountain Meals</div>
          </div>

          <div class="nav-links">
            <a href="#shop">Shop</a>
            <a href="#planner">Design Your Meal Plan</a>
            <a href="#about">About</a>
            <a href="#contact">Contact</a>
          </div>

          <div class="nav-cta">
            <button class="btn-primary">Shop Meals</button>
            <div class="language-pill">
              <span>EN</span><span>/</span><span>ES</span>
            </div>
          </div>
        </nav>
      </div>
    </header>

    <!-- HERO -->
    <main>
      <section class="hero">
        <div class="container hero-grid">
          <div class="hero-copy">
            <div class="hero-eyebrow">Freeze-dried Peruvian meals</div>
            <h1 class="hero-title">
              FOOD FROM THE MOUNTAINS, FOR THE MOUNTAINS.
            </h1>
            <p class="hero-subtitle">
              Real Peruvian ingredients, sourced from small Andean farmers, freeze-dried to travel light and taste big. Just add hot water and you’re back at the table — wherever your trail leads.
            </p>
            <div class="hero-actions">
              <button class="btn btn-primary">Shop Meals</button>
              <button class="btn btn-secondary">Design Your Meal Plan</button>
            </div>
            <p class="hero-note">
              Farm-direct sourcing · Whole ingredients · No preservatives
            </p>
          </div>

          <div class="hero-visual">
            <div class="hero-sun">
              <div class="hero-mountains"></div>
            </div>
            <div class="hero-label">
              <div class="hero-label-dot"></div>
              <span>Made in the Peruvian Andes</span>
            </div>
          </div>
        </div>
      </section>

      <!-- FEATURES / ICONS -->
      <section class="features">
        <div class="container">
          <div class="feature-grid">
            <article class="feature-card">
              <div class="feature-icon">🥕</div>
              <h3 class="feature-title">Farm-Direct Ingredients</h3>
              <p class="feature-text">Produce and staples sourced from small Peruvian farmers who know their soil and seasons.</p>
            </article>

            <article class="feature-card">
              <div class="feature-icon">❄️</div>
              <h3 class="feature-title">Freeze-Dried for Real Flavor</h3>
              <p class="feature-text">We lock in taste and nutrients with gentle freeze-drying — no preservatives, just real food.</p>
            </article>

            <article class="feature-card">
              <div class="feature-icon">🎒</div>
              <h3 class="feature-title">Designed for Adventure</h3>
              <p class="feature-text">Lightweight meals sized for summit pushes, thru-hikes, and everything in between.</p>
            </article>

            <article class="feature-card">
              <div class="feature-icon">♻️</div>
              <h3 class="feature-title">Sustainably Packaged</h3>
              <p class="feature-text">Give back to Earth with biodegradable packaging that keeps your pack light and your conscience lighter.</p>
            </article>
          </div>
        </div>
      </section>

      <!-- FEATURED MEALS -->
      <section class="meals" id="shop">
        <div class="container">
          <h2 class="section-heading">Featured Meals</h2>
          <p class="section-subtitle">
            Click a meal to see where every ingredient comes from in Peru and how the macros stack up to your day on the mountain.
          </p>

          <div class="meal-grid">
            <!-- Example meal card 1 -->
            <article class="meal-card">
              <div class="meal-thumb">
                <div class="meal-thumb-tag">High Protein</div>
              </div>
              <div class="meal-body">
                <h3 class="meal-title">Lomo Saltado Rice Bowl</h3>
                <p class="meal-meta">Beef · onions · ají amarillo · potatoes</p>
                <div class="meal-footer">
                  <span class="meal-link">View ingredients & macros</span>
                  <span class="meal-price">$14</span>
                </div>
              </div>
            </article>

            <!-- Example meal card 2 -->
            <article class="meal-card">
              <div class="meal-thumb">
                <div class="meal-thumb-tag">Vegan</div>
              </div>
              <div class="meal-body">
                <h3 class="meal-title">Quinoto de la Sierra</h3>
                <p class="meal-meta">Quinoa · seasonal veggies · herbs</p>
                <div class="meal-footer">
                  <span class="meal-link">View ingredients & macros</span>
                  <span class="meal-price">$13</span>
                </div>
              </div>
            </article>

            <!-- Example meal card 3 -->
            <article class="meal-card">
              <div class="meal-thumb">
                <div class="meal-thumb-tag">All-Day Energy</div>
              </div>
              <div class="meal-body">
                <h3 class="meal-title">Apu Breakfast Hash</h3>
                <p class="meal-meta">Andean potatoes · eggs · peppers</p>
                <div class="meal-footer">
                  <span class="meal-link">View ingredients & macros</span>
                  <span class="meal-price">$12</span>
                </div>
              </div>
            </article>
          </div>
        </div>
      </section>

      <!-- MEAL PLAN CTA -->
      <section class="planner" id="planner">
        <div class="container">
          <div class="planner-inner">
            <div class="planner-bg-sun"></div>
            <div class="planner-tag">Design your meal plan</div>
            <h2 class="planner-title">Build your perfect mountain menu.</h2>
            <p class="planner-text">
              Choose meals by calories, protein, diet type, and trip length — then see your total macros for each day on the trail. No spreadsheets, no guesswork, just a plan that keeps you moving.
            </p>
            <button class="btn btn-primary">Start Meal Planner</button>
            <p class="planner-meta">
              Coming soon: save presets for weekend trips, week-long treks, and summit-day pushes.
            </p>
          </div>
        </div>
      </section>

      <!-- STORY / ABOUT PREVIEW -->
      <section class="story" id="about">
        <div class="container story-grid">
          <div class="story-illustration">
            <div class="story-sun"></div>
            <div class="story-mountains"></div>
            <div class="story-label">
              <div class="story-label-pill"></div>
              <span>From Huaraz, Peru</span>
            </div>
          </div>

          <div>
            <h2 class="story-heading">Rooted in Peru. Made for the world’s mountains.</h2>
            <p class="story-body">
              These recipes start in the high valleys and markets of Peru: farmers we know by name, ingredients we’ve cooked with for years, and flavors that taste like home after a long day on the trail. We freeze-dry each meal at peak freshness so you can carry that comfort into the mountains — whether you’re crossing the Cordillera Blanca or camping out closer to home.
            </p>
            <p class="story-body">
              Every bag supports small-scale agriculture, responsible sourcing, and a lighter impact on the landscapes we love.
            </p>
            <a class="story-link" href="#about">Read our full story</a>
          </div>
        </div>
      </section>
    </main>

    <!-- FOOTER / CONTACT -->
    <footer id="contact">
      <div class="container footer-row">
        <div class="footer-nav">
          <span>&copy; <span id="year"></span> Mountain Meals</span>
          <a href="#shop">Shop</a>
          <a href="#planner">Meal Plan</a>
          <a href="#about">About</a>
          <a href="#contact">Contact</a>
        </div>
        <div class="footer-note">
          Built with real food, mountain miles, and a tiny freeze dryer.
        </div>
      </div>
    </footer>
  </div>

  <script>
    // Tiny helper to keep the year current
    document.getElementById("year").textContent = new Date().getFullYear();
  </script>
</body>
</html>
